// Service deprecated as per user request to remove AI features.
// File kept to avoid build errors if imported elsewhere, but effectively empty.
export const generateLoveMessage = async (): Promise<string> => {
  return "Features disabled";
};
